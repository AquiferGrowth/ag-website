---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

export async function getStaticPaths() {
	const caseStudies = await getCollection('caseStudies');
	return caseStudies.map(entry => ({
		params: { slug: entry.slug }, props: { entry },
	}));
}

const { entry } = Astro.props;
const { Content } = await entry.render();
const { title, headline, description, roi, info, industry, platform, image, challenges, solutions, results, highlights, clientUrl } = entry.data;
---

<Layout title={`${title} | Case Study`} description={description}>
	<Header />
	
	<main class="cs-page">
		<!-- Hero Section -->
		<section class="cs-hero">
			<div class="cs-container">
				<div class="cs-hero-grid">
					<div class="cs-hero-text">
						<h1>{title} {headline}</h1>
						<p class="cs-desc">{description}</p>

						{highlights && highlights.length > 0 && (
							<ul class="cs-highlights">
								{highlights.map(h => (
									<li>
										<span class="cs-check">
											<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
												<rect width="20" height="20" rx="4" fill="#E8364E"/>
												<path d="M6 10l2.5 2.5L14 7.5" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
											</svg>
										</span>
										<span>{h}</span>
									</li>
								))}
							</ul>
						)}
					</div>

					<div class="cs-hero-card">
						<div class="cs-card-inner">
							<a href={clientUrl} target="_blank" rel="noopener" class="cs-card-img-link">
								<img src={image} alt={title} />
								<div class="cs-card-img-overlay">
									<span>Visit Website ↗</span>
								</div>
							</a>
							<div class="cs-card-meta">
								<div class="cs-meta-row">
									<div class="cs-meta-item">
										<span class="cs-meta-label">Customer</span>
										<span class="cs-meta-value">{title}</span>
									</div>
									<div class="cs-meta-item">
										<span class="cs-meta-label">Industry</span>
										<span class="cs-meta-value">{industry}</span>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Timeline Section -->
		{(challenges || solutions || results) && (
			<section class="cs-timeline">
				<div class="cs-container">
					<div class="cs-timeline-inner">
						<!-- The Problem -->
						{challenges && challenges.length > 0 && (
							<div class="cs-tl-section">
								<div class="cs-tl-marker"></div>
								<div class="cs-tl-content">
									<h2>The Problem</h2>
									<p class="cs-tl-intro">
										Before working with Aquifer Growth, {title} struggled to keep up with the operational demands of their catalog. Manual processes couldn't scale.
									</p>
									{challenges.map(c => (
										<div class="cs-tl-item">
											<h3>{c.title}</h3>
											<p>{c.body}</p>
										</div>
									))}
								</div>
							</div>
						)}

						<!-- The Solution -->
						{solutions && solutions.length > 0 && (
							<div class="cs-tl-section">
								<div class="cs-tl-marker"></div>
								<div class="cs-tl-content">
									<h2>The Solution</h2>
									{solutions.map(s => (
										<div class="cs-tl-item">
											<h3>{s.title}</h3>
											<p>{s.body}</p>
										</div>
									))}
								</div>
							</div>
						)}

						<!-- The Results -->
						{results && (
							<div class="cs-tl-section">
								<div class="cs-tl-marker"></div>
								<div class="cs-tl-content">
									<h2>The Results</h2>
									<p class="cs-tl-results">{results}</p>
								</div>
							</div>
						)}
					</div>
				</div>
			</section>
		)}
	</main>

	<Footer />
</Layout>

<style>
	/* ─── Page ─── */
	.cs-page {
		padding-top: 2rem;
	}
	/* Reset heavy global heading defaults for this page */
	.cs-page h1, .cs-page h2, .cs-page h3 {
		font-family: var(--font-sans);
		text-transform: none;
		letter-spacing: normal;
		line-height: 1.3;
	}
	.cs-container {
		max-width: 1100px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	/* ─── Hero ─── */
	.cs-hero {
		padding: 3rem 0 4rem;
		background: linear-gradient(180deg, #faf9f7 0%, #fff5f5 100%);
	}

	.cs-hero-grid {
		display: grid;
		grid-template-columns: 1fr 380px;
		gap: 3rem;
		align-items: start;
	}

	.cs-hero-text h1 {
		font-size: clamp(1.6rem, 3.5vw, 2.2rem);
		font-weight: 600;
		letter-spacing: -0.02em;
		line-height: 1.3;
		color: #1a1a1a;
		margin-bottom: 1.5rem;
	}

	.cs-desc {
		font-size: 0.9rem;
		line-height: 1.75;
		color: #555;
		margin-bottom: 2rem;
	}

	/* Highlights (checkmark list) */
	.cs-highlights {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}
	.cs-highlights li {
		display: flex;
		align-items: center;
		gap: 0.65rem;
		font-size: 0.88rem;
		font-weight: 400;
		color: #333;
		line-height: 1.4;
	}
	.cs-check {
		flex-shrink: 0;
		display: flex;
		align-items: center;
	}

	/* ─── Info Card ─── */
	.cs-card-inner {
		background: #fff;
		border-radius: 16px;
		overflow: hidden;
		box-shadow: 0 8px 30px rgba(0,0,0,0.06);
		border: 1px solid rgba(0,0,0,0.06);
	}
	.cs-card-img-link {
		display: block;
		height: 200px;
		overflow: hidden;
		position: relative;
		text-decoration: none;
	}
	.cs-card-img-link img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: transform 0.4s ease;
	}
	.cs-card-img-link:hover img {
		transform: scale(1.05);
	}
	.cs-card-img-overlay {
		position: absolute;
		inset: 0;
		background: rgba(0,0,0,0.45);
		display: flex;
		align-items: center;
		justify-content: center;
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	.cs-card-img-link:hover .cs-card-img-overlay {
		opacity: 1;
	}
	.cs-card-img-overlay span {
		color: #fff;
		font-size: 0.85rem;
		font-weight: 700;
		letter-spacing: 0.02em;
	}
	.cs-card-meta {
		padding: 1.25rem 1.5rem 1.5rem;
	}
	.cs-meta-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
	}
	.cs-meta-label {
		display: block;
		font-size: 0.7rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-weight: 600;
		color: #999;
		margin-bottom: 0.2rem;
	}
	.cs-meta-value {
		display: block;
		font-size: 0.9rem;
		font-weight: 700;
		color: #1a1a1a;
	}

	/* ─── Timeline ─── */
	.cs-timeline {
		padding: 3rem 0 5rem;
		background: #fff;
	}

	.cs-timeline-inner {
		position: relative;
		padding-left: 3rem;
	}

	/* Vertical line */
	.cs-timeline-inner::before {
		content: '';
		position: absolute;
		left: 8px;
		top: 10px;
		bottom: 10px;
		width: 2px;
		background: #e5e5e5;
	}

	.cs-tl-section {
		position: relative;
		margin-bottom: 3rem;
	}
	.cs-tl-section:last-child {
		margin-bottom: 0;
	}

	/* Red dot marker */
	.cs-tl-marker {
		position: absolute;
		left: -3rem;
		top: 0.35rem;
		width: 18px;
		height: 18px;
		border-radius: 50%;
		background: #E8364E;
		transform: translateX(-1px);
	}

	.cs-tl-content h2 {
		font-size: 1.35rem;
		font-weight: 600;
		color: #1a1a1a;
		margin-bottom: 1rem;
		letter-spacing: -0.01em;
		text-transform: none;
	}

	.cs-tl-intro {
		font-size: 0.9rem;
		line-height: 1.75;
		color: #555;
		margin-bottom: 1.75rem;
	}

	.cs-tl-item {
		margin-bottom: 1.5rem;
	}
	.cs-tl-item:last-child {
		margin-bottom: 0;
	}
	.cs-tl-item h3 {
		font-size: 1rem;
		font-weight: 650;
		color: #1a1a1a;
		margin-bottom: 0.35rem;
		text-transform: none;
		letter-spacing: normal;
	}
	.cs-tl-item p {
		font-size: 0.9rem;
		line-height: 1.75;
		color: #555;
	}

	.cs-tl-results {
		font-size: 0.9rem;
		line-height: 1.75;
		color: #444;
	}

	/* ─── Responsive ─── */
	@media (max-width: 768px) {
		.cs-hero-grid {
			grid-template-columns: 1fr;
		}
		.cs-hero-text h1 {
			font-size: 1.5rem;
		}
		.cs-timeline-inner {
			padding-left: 2.5rem;
		}
		.cs-tl-marker {
			left: -2.5rem;
		}
	}
</style>
